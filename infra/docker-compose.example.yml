version: "3.9"

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: macroarena
      POSTGRES_USER: macroarena
      POSTGRES_PASSWORD: macroarena
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

volumes:
  pg_data:
